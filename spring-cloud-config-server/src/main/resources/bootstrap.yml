server:
  port: 8889

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///work/java/spring-cloud-demo/config-repo
          searchPaths: '{application}'
          force-pull: true
